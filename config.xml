<?xml version="1.0" encoding="UTF-8"?>

<!-- config.xml reference: https://build.phonegap.com/docs/config-xml -->
<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id        = "com.nortech.blitz"
        version   = "0.0.14">

    <name>WebIT Mobile</name>

    <description>
        WebIT Mobile Summary Reporting Platform
    </description>

    <author href="http://www.myfootcount.com" email="nortechdev@gmail.com">
        Nortech Software Team
    </author>

    <!--
        If you do not want any permissions to be added to your app, add the
        following tag to your config.xml; you will still have the INTERNET
        permission on your app, which PhoneGap requires.
    -->
    <preference name="permissions"                value="none"/>

    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version"           value="3.6.3" />          <!-- all: current version of PhoneGap -->
    <preference name="orientation"                value="default" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- ios: possible values handset, tablet, or universal; by default, all builds are universal -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="true" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="true" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="true" />           <!-- ios: 'false' means that external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="true" />           <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="false" />          <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="7" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

    <!-- Plugins -->

    <!-- Core plugins -->
    <gap:plugin name="org.apache.cordova.inappbrowser" version="0.5.2" />           <!--Web browser view that displays when opening links or calling window.open()-->
    <gap:plugin name="org.apache.cordova.network-information" version="0.2.12" />   <!--Connection object for info about device's cellular and wifi connection-->
    <gap:plugin name="org.apache.cordova.splashscreen" version="0.3.3" />           <!--Displays and hides the application's splash screen-->
    <gap:plugin name="org.apache.cordova.dialogs" version="0.2.10" />               <!--Visual device notifications-->

    <!-- Third party plugins -->
    <!-- A list of available plugins are available at https://build.phonegap.com/plugins -->
    <!-- 
        <gap:plugin name="com.phonegap.plugins.barcodescanner" />
    -->

    <!-- Define app icon for each platform. -->
    <!-- Default -->
            <icon src="icon.png" /> <!-- 128x128 px -->
    <!-- Android -->
            <icon src="res/icon/android/ldpi.png"   gap:platform="android"    gap:density="ldpi" /> <!-- 36x36 px -->
            <icon src="res/icon/android/mdpi.png"   gap:platform="android"    gap:density="mdpi" /> <!-- 48x48 px -->
            <icon src="res/icon/android/hdpi.png"   gap:platform="android"    gap:density="hdpi" /> <!-- 72x72 px -->
            <icon src="res/icon/android/xhdpi.png"  gap:platform="android"    gap:density="xhdpi" /> <!-- 96x96 px -->
    <!-- BlackBerry 10-->
            <icon src="res/icon/bb10/icon-86.png"     gap:platform="blackberry10"/> <!-- 86x86 px -->
            <icon src="res/icon/bb10/icon-150.png"    gap:platform="blackberry10"/> <!-- 150x150 px -->
    <!-- Windows Phone 8 -->
            <icon src="res/icon/wp/ApplicationIcon.png"     gap:platform="wp8" width="99" height="99" />
            <icon src="res/icon/wp/Background.png"          gap:platform="wp8" width="159" height="159" />
    <!-- Windows 8 -->
            <icon src="res/icon/windows8/logo.png"          gap:platform="windows8" width="150" height="150" />
            <icon src="res/icon/windows8/smalllogo.png"     gap:platform="windows8" width="30" height="30" />
            <icon src="res/icon/windows8/storelogo.png"     gap:platform="windows8" width="50" height="50" />
    <!-- iOS -->
        <!-- iOS 7.0+ -->
            <!-- iPhone / iPod Touch  -->
            <icon src="res/icon/ios/icon-60.png"        gap:platform="ios" width="60" height="60" />
            <icon src="res/icon/ios/icon-60@2x.png"     gap:platform="ios" width="120" height="120" />
            <!-- iPad -->
            <icon src="res/icon/ios/icon-76.png"        gap:platform="ios" width="76" height="76" />
            <icon src="res/icon/ios/icon-76@2x.png"     gap:platform="ios" width="152" height="152" />
        <!-- iOS 6.1-->
            <!-- iPhone / iPod Touch -->
            <icon src="res/icon/ios/icon.png"           gap:platform="ios" width="57" height="57" />
            <icon src="res/icon/ios/icon@2x.png"        gap:platform="ios" width="114" height="114" />
            <!-- iPad -->
            <icon src="res/icon/ios/icon-72.png"        gap:platform="ios" width="72" height="72" />
            <icon src="res/icon/ios/icon-72@2x.png"     gap:platform="ios" width="144" height="144" />

    <!-- Define app splash screen for each platform. Name of source image is not important - will be renamed internally by Cordova -->
    <!-- Android -->
        <!-- you can use any density that exists in the Android project -->
        <splash src="res/screen/android/splash-land-hdpi.png"   gap:platform="android" gap:density="land-hdpi"/> <!-- 800x480 px -->
        <splash src="res/screen/android/splash-land-ldpi.png"   gap:platform="android" gap:density="land-ldpi"/> <!--320x200 px -->
        <splash src="res/screen/android/splash-land-mdpi.png"   gap:platform="android" gap:density="land-mdpi"/> <!-- 480x320 px -->
        <splash src="res/screen/android/splash-land-xhdpi.png"  gap:platform="android" gap:density="land-xhdpi"/> <!-- 1280x720 px --> 

        <splash src="res/screen/android/splash-port-hdpi.png"   gap:platform="android" gap:density="port-hdpi"/> <!--480x800 px -->
        <splash src="res/screen/android/splash-port-ldpi.png"   gap:platform="android" gap:density="port-ldpi"/> <!-- 200x320 px -->
        <splash src="res/screen/android/splash-port-mdpi.png"   gap:platform="android" gap:density="port-mdpi"/> <!--320x480 px -->
        <splash src="res/screen/android/splash-port-xhdpi.png"  gap:platform="android" gap:density="port-xhdpi"/> <!-- 720x1280 px -->

	<!-- BlackBerry -->
        <splash src="res/screen/bb10/splash-1024x600.png"   gap:platform="blackberry" />
        <splash src="res/screen/bb10/splash-600x1024.png"   gap:platform="blackberry" />
        <splash src="res/screen/bb10/splash-1280x768.png"   gap:platform="blackberry" />
        <splash src="res/screen/bb10/splash-768x1280.png"   gap:platform="blackberry" />
	
    <!-- iOS -->
        <!-- images are determined by width and height. The following are supported -->
        <!-- iOS: iPhone and iPod touch -->
            <splash src="res/screen/ios/Default~iphone.png"             gap:platform="ios" width="320" height="480"/>
            <splash src="res/screen/ios/Default@2x~iphone.png"          gap:platform="ios" width="640" height="960"/>
        <!-- iPad -->
            <splash src="res/screen/ios/Default-Portrait~ipad.png"      gap:platform="ios" width="768" height="1024"/>
            <splash src="res/screen/ios/Default-Landscape~ipad.png"     gap:platform="ios" width="1024" height="768"/>
        <!-- Retina iPad -->
            <splash src="res/screen/ios/Default-Portrait@2x~ipad.png"   gap:platform="ios" width="1536" height="2048"/>
            <splash src="res/screen/ios/Default-Landscape@2x~ipad.png"  gap:platform="ios" width="2048" height="1536"/>
        <!-- iPhone 5 / iPod Touch (5th Generation) -->
            <splash src="res/screen/ios/Default-568h@2x~iphone.png"     gap:platform="ios" width="640" height="1136"/>

    <!-- Windows Phone 8 -->
        <!-- images are determined by width and height. The following are supported -->
        <splash src="res/screen/wp/SplashScreenImage.jpg"      gap:platform="wp8" width="768" height="1280"/>
    
    <!-- Windows 8 -->
        <!-- images are determined by width and height. The following are supported -->
        <splash src="res/screen/windows8/splashscreen.png"      gap:platform="windows8" width="620" height="300"/>

    <!--
        Define access to external domains.

        <access />            - a blank access tag denies access to all external resources.
        <access origin="*" /> - a wildcard access tag allows access to all external resource.

        Otherwise, you can specify specific domains:
    -->
    <access origin="*"/> <!-- allow local pages -->
    <!--
        <access origin="http://phonegap.com" />                    - allow any secure requests to http://phonegap.com/
        <access origin="http://phonegap.com" subdomains="true" />  - same as above, but including subdomains, such as http://build.phonegap.com/
    -->

</widget>